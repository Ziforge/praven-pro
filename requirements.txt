# Core dependencies - REQUIRED
pandas>=2.0.0           # CSV processing and data manipulation
numpy>=1.24.0           # Numerical operations
requests>=2.31.0        # API calls (eBird, GBIF, OSM, Open-Meteo)
pydantic>=2.0.0         # Configuration validation

# Web interface - OPTIONAL (only if using web_app.py)
flask>=2.0.0            # Web server

# Visualization - OPTIONAL (only if generating dashboards)
plotly>=5.0.0           # Interactive charts
matplotlib>=3.5.0       # Static charts

# Machine learning - OPTIONAL (only if using weather model)
scikit-learn>=1.0.0     # Smart review selection
